<!DOCTYPE html>
<html>
  <head>
    <%- include('partials/head') %>
  </head>
  <body>
    <%- include('partials/masthead') %>
    <div class="container">
      <h2>(1/3) Attendee log-in</h2>

      <p>
        Below is an example copy of the liability release, it is also available
        at <a href="/example.pdf">/example.pdf</a><br /><br />
        After reading the waiver (with your parent), please continue forward
        with the signing process
      </p>

      <iframe
        class="intro"
        src="/example.pdf"
        width="100%"
        height="300"
      ></iframe>

      <form action="/login" method="POST">
        <div class="form-row">
          <div class="form-label">
            <label for="name"
              >Please enter your confirmation code (you can find that on your
              registration confirmation)</label
            >
          </div>
          <div class="form-field">
            <input
              type="text"
              id="ordercode"
              name="ordercode"
              class="c-input js-order-code"
              size="40"
              maxlength="6"
              placeholder="90XAGJ"
              oninvalid="this.setCustomValidity('Please a valid confirmation code. Confirmation codes are 6 characters long and contain numbers & letters')"
              oninput="setCustomValidity(''); document.querySelector('.js-login').disabled = this.value.length < 6;"
              required
            />
          </div>
        </div>

        <div class="form-row">
          <p>
            We will send a message with a link to start the signing process to
            the attendee email we have on file for you.
            <br />If there's any issue with your registration, please contact
            <a href="mailto:charalampos@hackthethames.uk"
              >charalampos@hackthethames.uk</a
            >
          </p>
        </div>

        <div class="form-submit">
          <input
            class="c-submit js-login"
            type="submit"
            value="Begin waiver signing"
            disabled
          />
        </div>
      </form>
    </div>

    <%- include('partials/footer') %>
  </body>
</html>
